
<form class="paper_edit_form" action="{{ path('paper_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    <input type="hidden" name="_method" value="PUT" />
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit" class="paper_submit" onclick="return paper_form(this);">Edit</button>
    </p>
</form>
