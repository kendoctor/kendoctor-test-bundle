{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ItemGroup list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>Creaetedat</th>
                <th>Weight</th>
                <th>Type</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('itemgroup_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.description }}</td>
                <td>{% if entity.creaetedAt %}{{ entity.creaetedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.weight }}</td>
                <td>{{ entity.type }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('itemgroup_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('itemgroup_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('itemgroup_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
